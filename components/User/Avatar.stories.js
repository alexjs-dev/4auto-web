import Component from './Avatar.js'

export default {
  title: 'Avatar',
  component: Component,
}

const Template = (args) => <Component {...args} />

export const Avatar = Template.bind({})
Avatar.args = {}
