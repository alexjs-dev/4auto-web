import Component from './SearchModal.js'

export default {
  title: ' Search Modal',
  component: Component,
}

const Template = (args) => <Component {...args} />

export const SearchModal = Template.bind({})
SearchModal.args = {}
