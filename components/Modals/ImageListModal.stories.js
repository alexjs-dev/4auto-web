import Component from './ImageListModal.js'

export default {
  title: ' Image List Modal',
  component: Component,
}

const Template = (args) => <Component {...args} />

export const ImageListModal = Template.bind({})
ImageListModal.args = {}
